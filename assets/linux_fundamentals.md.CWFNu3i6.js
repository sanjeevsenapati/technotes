import{_ as a,o as s,c as t,a2 as i}from"./chunks/framework.Ddyno5UB.js";const p=JSON.parse('{"title":"1. Linux Fundamentals","description":"","frontmatter":{},"headers":[],"relativePath":"linux/fundamentals.md","filePath":"linux/fundamentals.md"}'),n={name:"linux/fundamentals.md"};function o(r,e,l,d,c,h){return s(),t("div",null,[...e[0]||(e[0]=[i('<h1 id="_1-linux-fundamentals" tabindex="-1">1. Linux Fundamentals <a class="header-anchor" href="#_1-linux-fundamentals" aria-label="Permalink to &quot;1. Linux Fundamentals&quot;">​</a></h1><p>Understanding the core architecture of Linux is crucial for system administration, containerization, and backend development. Linux is not just an operating system; it is a kernel around which various GNU utilities and applications are built.</p><h2 id="the-kernel-vs-user-space" tabindex="-1">The Kernel vs. User Space <a class="header-anchor" href="#the-kernel-vs-user-space" aria-label="Permalink to &quot;The Kernel vs. User Space&quot;">​</a></h2><p>The Linux operating system is divided into two primary memory and execution areas:</p><ol><li><strong>Kernel Space</strong>: The core of the operating system. It has complete and unrestricted access to the underlying hardware. It manages memory, processes, device drivers, and system calls.</li><li><strong>User Space</strong>: Where user applications (like your shell, web servers, and databases) run. Applications in user space cannot access hardware directly; they must communicate with the Kernel via <strong>System Calls</strong> (syscalls).</li></ol><p>This separation is what makes Linux highly stable and secure. If an application in User Space crashes, it cannot easily take down the Kernel.</p><h2 id="the-boot-process" tabindex="-1">The Boot Process <a class="header-anchor" href="#the-boot-process" aria-label="Permalink to &quot;The Boot Process&quot;">​</a></h2><p>When a Linux server powers on, it goes through a specific sequence:</p><ol><li><strong>BIOS/UEFI</strong>: The hardware initializes and looks for a bootable device.</li><li><strong>Bootloader (GRUB)</strong>: Loads the selected Kernel into memory and executes it.</li><li><strong>Kernel Initialization</strong>: The Kernel takes over, mounts the root filesystem (<code>/</code>), and initializes hardware drivers.</li><li><strong>Init System (Systemd)</strong>: The Kernel executes the very first User Space process, which has Process ID (PID) 1. On modern Linux, this is almost always <code>systemd</code>. <code>systemd</code> is then responsible for starting all other services (networking, SSH, databases).</li></ol><h2 id="the-unix-philosophy-everything-is-a-file" tabindex="-1">The Unix Philosophy: &quot;Everything is a File&quot; <a class="header-anchor" href="#the-unix-philosophy-everything-is-a-file" aria-label="Permalink to &quot;The Unix Philosophy: &quot;Everything is a File&quot;&quot;">​</a></h2><p>In Linux, the concept of a &quot;file&quot; extends far beyond standard text documents or images.</p><ul><li>A hard drive is a file (e.g., <code>/dev/sda</code>).</li><li>A terminal session is a file (e.g., <code>/dev/tty</code>).</li><li>System memory and process information are exposed as files (e.g., <code>/proc/meminfo</code>).</li></ul><p>This design means that standard tools used for reading and writing text files (<code>cat</code>, <code>echo</code>, <code>grep</code>) can also be used to interact with hardware and kernel parameters.</p>',13)])])}const m=a(n,[["render",o]]);export{p as __pageData,m as default};
