import{_ as a,o as i,c as s,a2 as n}from"./chunks/framework.CsFjlSKj.js";const d=JSON.parse('{"title":"Branching and Merging","description":"","frontmatter":{},"headers":[],"relativePath":"git/2-branching-and-merging.md","filePath":"git/2-branching-and-merging.md"}'),t={name:"git/2-branching-and-merging.md"};function l(o,e,r,h,c,p){return i(),s("div",null,[...e[0]||(e[0]=[n(`<h1 id="branching-and-merging" tabindex="-1">Branching and Merging <a class="header-anchor" href="#branching-and-merging" aria-label="Permalink to &quot;Branching and Merging&quot;">​</a></h1><p>One of Git&#39;s most powerful features is its nearly instantaneous branching mechanism. A branch in Git is simply a lightweight movable pointer to a specific commit.</p><h2 id="branch-management" tabindex="-1">Branch Management <a class="header-anchor" href="#branch-management" aria-label="Permalink to &quot;Branch Management&quot;">​</a></h2><p>The default branch name in Git is usually <code>master</code> or <code>main</code>. Whenever you start working on a new feature or bug fix, you should create a new branch to isolate your changes.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># List all local branches</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> branch</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Create a new branch called &quot;feature-login&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> branch</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> feature-login</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Switch your Working Directory to that branch</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> checkout</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> feature-login</span></span></code></pre></div><div class="tip custom-block github-alert"><p class="custom-block-title">TIP</p><p><strong>Checkout vs Switch:</strong> <code>git checkout</code> is an older, overloaded command that can switch branches <em>and</em> restore files. In newer versions of Git (2.23+), you can use <code>git switch feature-login</code> which is safer and designed specifically for changing branches.</p></div><p><strong>Shortcut:</strong> Create and switch to a new branch in one command:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> checkout</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -b</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> feature-login</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># or</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> switch</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> feature-login</span></span></code></pre></div><h2 id="merging" tabindex="-1">Merging <a class="header-anchor" href="#merging" aria-label="Permalink to &quot;Merging&quot;">​</a></h2><p>Once you have finished your work on <code>feature-login</code> and committed the changes, you need to merge them back into <code>main</code>.</p><ol><li>Switch back to the receiving branch (<code>main</code>).</li><li>Run the merge command, pointing to the branch you want to pull in.</li></ol><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Switch to main</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> switch</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> main</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Merge the feature branch into main</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> merge</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> feature-login</span></span></code></pre></div><h3 id="fast-forward-vs-recursive-merges" tabindex="-1">Fast-Forward vs Recursive Merges <a class="header-anchor" href="#fast-forward-vs-recursive-merges" aria-label="Permalink to &quot;Fast-Forward vs Recursive Merges&quot;">​</a></h3><ul><li><strong>Fast-Forward:</strong> If <code>main</code> has not changed since you created <code>feature-login</code>, Git simply moves the <code>main</code> pointer forward to match <code>feature-login</code>. No new merge commit is created.</li><li><strong>Recursive (3-Way) Merge:</strong> If <code>main</code> <em>has</em> changed (e.g., someone else merged a different feature), Git will attempt to automatically combine the changes and create a special &quot;Merge Commit&quot;.</li></ul><h2 id="resolving-merge-conflicts" tabindex="-1">Resolving Merge Conflicts <a class="header-anchor" href="#resolving-merge-conflicts" aria-label="Permalink to &quot;Resolving Merge Conflicts&quot;">​</a></h2><p>If you and another developer edited the exact same line of the exact same file on different branches, Git cannot automatically merge them. It will pause the merge and declare a <strong>Merge Conflict</strong>.</p><p>If you run <code>git status</code>, it will list the conflicting files as &quot;both modified&quot;. If you open the conflicting file in your text editor, Git will have injected conflict markers:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Here is some text in the file.</span></span>
<span class="line"><span>&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD</span></span>
<span class="line"><span>This is the line on the &#39;main&#39; branch (where you currently are).</span></span>
<span class="line"><span>=======</span></span>
<span class="line"><span>This is the line on the &#39;feature-login&#39; branch (what you are trying to merge).</span></span>
<span class="line"><span>&gt;&gt;&gt;&gt;&gt;&gt;&gt; feature-login</span></span>
<span class="line"><span>And here is more text.</span></span></code></pre></div><p><strong>How to resolve it:</strong></p><ol><li>Manually edit the file to look exactly how you want the final version to look (delete the <code>&lt;&lt;&lt;&lt;&lt;&lt;&lt;</code>, <code>=======</code>, and <code>&gt;&gt;&gt;&gt;&gt;&gt;&gt;</code> markers).</li><li>Save the file.</li><li>Add the file to the staging area to tell Git the conflict is resolved: <code>git add &lt;file&gt;</code>.</li><li>Finish the merge by committing: <code>git commit</code>.</li></ol>`,20)])])}const k=a(t,[["render",l]]);export{d as __pageData,k as default};
